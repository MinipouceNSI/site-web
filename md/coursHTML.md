### Le langage HTML

1. HTML <br>

HyperText Markup Language *Langage de balises hypertexte*

Un fichier `.html` est un fichier de texte (comme le markdown par exemple). <br>
On ouvre un fichier de deux façons :
- Le développeur avec un éditeur de code (VsCode)
- Un utilisateur avec un navigateur
